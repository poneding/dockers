# ubuntud

## Build

```bash
./build.sh
```

## Run

start the container:

```bash
docker run -d --name ubt poneding/ubuntud:2204
```

exec into the container:

```bash
docker exec -it ubt /bin/bash
```

stop the container:

```bash
docker stop ubt
```
